package dev.pinaki.receepee.common.ds

/**
 * An object signify nothing.
 *
 * This is handy in cases where a [OneTimeEvent] needs to be sent through
 * a bus/livedata but no value is needed along with that
 */
object Empty